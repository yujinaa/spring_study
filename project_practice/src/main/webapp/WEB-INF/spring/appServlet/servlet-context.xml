<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
	xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing 
		infrastructure -->

	<!-- Enables the Spring MVC @Controller programming model -->
	<annotation-driven />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving 
		up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources 
		in the /WEB-INF/views directory -->
	<beans:bean
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>

	<context:component-scan
		base-package="com.care.root" />

	<beans:bean id="memberInterceptor"
		class="com.care.root.member.interceptor.MemberInterceptor" /> <!-- class = "자료형" id = "변수명" -->

	<beans:bean id="bti"
		class="com.care.root.testboard.BoardTestInterceptor" /> <!-- class = "자료형" id = "변수명" -->

	<beans:bean id="autoLogin"
		class="com.care.root.member.interceptor.AutoLoginInterceptor" />


	<beans:bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<beans:property name="maxUploadSize" value="52428800" />
		<beans:property name="defaultEncoding" value="utf-8" />
	</beans:bean>
	
	
	<interceptors>
		<interceptor>
			<mapping path="/index" /> <!-- 같은 인터셉터 사용가능 -->
			<beans:ref bean="autoLogin" /> <!-- memberInterceptor 동작시켜라 -->
		</interceptor>

		<interceptor>
			<!-- <mapping path="/index"/> index 요청이 되면 -->
			<mapping path="/member/memberInfo" /> <!-- memberInfo 요청하면 -->
			<mapping path="/board/writeForm" />
			<beans:ref bean="memberInterceptor" /> <!-- memberInterceptor 동작시켜라 -->
		</interceptor>

		<interceptor>
			<mapping path="/test/board" />
			<mapping path="/test/write" />
			<beans:ref bean="bti" />
		</interceptor>

	</interceptors>


</beans:beans>